{
	"name": "async-merge-calls",
	"version": "0.1.0",
	"description": "Merge asynchronous calls with other duplicate calls in progress",
	"main": "index.js",
	"repository": "git@github.com:mikecote/async-merge-calls.git",
	"author": "Mike Côté",
	"license": "MIT",
	"private": false,
	"scripts": {
		"lint": "eslint .",
		"test": "yarn run lint && yarn run jest",
		"jest": "jest"
	},
	"devDependencies": {
		"async": "^2.6.1",
		"eslint": "4.19.1",
		"eslint-config-airbnb-base": "^13.0.0",
		"eslint-plugin-import": "^2.13.0",
		"jest": "^23.4.2"
	},
	"jest": {
		"testEnvironment": "node"
	}
}
